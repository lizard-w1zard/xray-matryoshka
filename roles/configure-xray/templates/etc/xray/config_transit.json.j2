{
  "log": { "loglevel": "warning" },
  "inbounds": [
    {
      "port": 443,
      "protocol": "vless",
      "settings": {
        "clients": [{ "id": "{{ xray_transit_uuid }}", "flow": "" }],
        "decryption": "none"
      },
      "streamSettings": {
        "network": "grpc",
        "security": "reality",
        "realitySettings": {
          "dest": "{{ xray_transit_dest }}:443",
          "serverNames": ["{{ xray_transit_dest }}"],
          "privateKey": "{{ xray_transit_private_key }}",
          "shortIds": ["{{ xray_transit_short_id }}"]
        },
        "grpcSettings": {
          "serviceName": "grpc"
        }
      }
    }
  ],
  "outbounds": [
    {
      "tag": "forward_to_exit",
      "protocol": "vless",
      "settings": {
        "vnext": [
          {
            "address": "{{ hostvars['exit']['ansible_host'] }}",
            "port": 443,
            "users": [
              {
                "id": "{{ xray_exit_uuid }}",
                "encryption": "none",
                "flow": ""
              }
            ]
          }
        ]
      },
      "streamSettings": {
        "network": "tcp",
        "security": "none"
      }
    }
  ]
}